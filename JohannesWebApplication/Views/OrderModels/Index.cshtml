@model IEnumerable<JohannesWebApplication.Models.OrderModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Infill)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PrintFilePath)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Infill)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PrintFilePath)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PrinterID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PrinterID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PrinterID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
